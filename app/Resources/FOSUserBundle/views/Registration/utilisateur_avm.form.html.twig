
{% block fos_user_content %}
    <h1>Création de compte Easy-trade</h1>
    {% trans_default_domain 'FOSUserBundle' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'registration.submit'|trans }}" />
        </div>
        {{ form_end(form) }}
{% endblock fos_user_content %}