{% extends "APMAdminBundle:_Main:layout.html.twig" %}

{% block body %}
    <h1>Attribuer des Commissionnements</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('apm_marketing_commissionnement_index', {'id': boutique.id}) }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
