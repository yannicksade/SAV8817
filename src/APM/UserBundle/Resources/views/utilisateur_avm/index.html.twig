{% extends "APMAdminBundle:_Main:layout.html.twig" %}

{% block body %}
  Groupe: {{ groupe }}
    <h1 xmlns="http://www.w3.org/1999/html"> Liste des utilisateurs</h1>
    {% for user in users %}
        <a href="{{ path('apm_user_profile_show', {'id':user.id}) }}">{{ user }}</a><br/>
    {% endfor %}

{% endblock %}